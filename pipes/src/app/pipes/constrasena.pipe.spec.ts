import { ConstrasenaPipe } from './constrasena.pipe';

describe('ConstrasenaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConstrasenaPipe();
    expect(pipe).toBeTruthy();
  });
});
